class World {
  // Erstellt  neue Objects.
  character = new Character();
  enemies = [new Chicken(), new Chicken(), new Chicken(), new Chick(), new Chick(), new Chick()]; // Fügt der Variable 3 Chicken hinzu.

  draw() {
    // Zeichnet Objekte
  }
}
